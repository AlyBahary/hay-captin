<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewHolder"
            type="com.beetleware.hayatiDeliveryMan.ui.adapter.UpdateStatusAdapter.UpdateStatusViewHolder" />
    </data>

    <androidx.cardview.widget.CardView
        android:id="@+id/container_for_pay_type"
        app:cardCornerRadius="@dimen/space_10"
        android:layout_margin="@dimen/space_8"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RelativeLayout
            android:padding="@dimen/x7dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/img_payment"
                android:layout_width="@dimen/x30dp"
                android:layout_height="@dimen/x30dp"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:layout_marginStart="@dimen/space_10"
                android:src="@drawable/ic_maps"
                app:tint="@android:color/black" />

            <TextView
                android:id="@+id/status_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginStart="@dimen/space_15"
                android:layout_toEndOf="@id/img_payment"
                android:fontFamily="@font/questive_bold"
                android:textColor="@android:color/black"
                android:textSize="@dimen/x12sp"
                tools:text="بطاقه ائتمان" />

            <com.google.android.material.radiobutton.MaterialRadioButton
                android:id="@+id/radio_status"
                android:layout_alignParentEnd="true"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        </RelativeLayout>
    </androidx.cardview.widget.CardView>

</layout>
